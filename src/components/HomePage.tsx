import Page from "./Page";
import DealerList from "./DealerList";

const HomePage = (  ) => {
  return (
    <Page title="Dealer List">
      <DealerList />
    </Page>
  );
};

export default HomePage;
